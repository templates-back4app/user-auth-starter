// Stack: Flutter config | File: lib/config.dart (copy of config.example.dart — config.dart is gitignored)
// Both values come from the backend's Overview page. The client key is meant to ship inside the app.
const appId = 'your-app-id';
const clientKey = 'your-client-key';
const backendUrl = 'https://parseapi.back4app.com';
